<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">    
<mapper namespace="io.grx.modules.sys.dao.SysConfigDao">
	<select id="queryObject" resultType="io.grx.modules.sys.entity.SysConfigEntity">
		select * from sys_config where id = #{id}
	</select>

	<select id="queryObjectBySysConfigEntity" parameterType="io.grx.modules.sys.entity.SysConfigEntity" resultType="io.grx.modules.sys.entity.SysConfigEntity">
		select * from sys_config where 1=1
		<if test="merchantNo != null and merchantNo != ''">
			and `merchant_no` =#{merchantNo}
		</if>
		<if test="key != null and key != ''">
			and `key` =#{key}
		</if>
	</select>
	
	<select id="queryList" parameterType="map" resultType="io.grx.modules.sys.entity.SysConfigEntity">
		select * from sys_config where status = 1 and merchant_no = #{_merchantNo}
		<if test="key != null and key.trim() != ''">
			and `key` like concat('%',#{key},'%') 
		</if>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
	<select id="queryTotal" parameterType="map" resultType="int">
		select count(*) from sys_config where status = 1 and merchant_no = #{_merchantNo}
		<if test="key != null and key.trim() != ''">
			and `key` like concat('%',#{key},'%')
		</if>
	</select>
	
	<insert id="save" parameterType="io.grx.modules.sys.entity.SysConfigEntity">
		insert into sys_config(`key`, `value`, remark, merchant_no)
			values(#{key},#{value},#{remark},#{merchantNo})
	</insert>
	
	<update id="update" parameterType="io.grx.modules.sys.entity.SysConfigEntity">
		update sys_config set `key` = #{key}, `value` = #{value}, remark = #{remark} 
			 where id = #{id}
	</update>
	
	<!-- 根据key，更新value -->
	<update id="updateValueByKey" parameterType="map">
		update sys_config set `value` = #{value} where `key` = #{key} and merchant_no = #{_merchantNo}
	</update>
	
	<!-- 根据key，查询value -->
	<select id="queryByKey" parameterType="map" resultType="io.grx.modules.sys.entity.SysConfigEntity">
		select * from sys_config where `key` = #{key} and merchant_no = #{merchantNo}
	</select>
	
	<delete id="deleteBatch" parameterType="int">
		delete from sys_config where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>